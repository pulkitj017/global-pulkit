# Frontend (React + Vite)

```bash
cd /Users/pulkit.jain8004/Documents/FinTech\ POC/frontend
npm install
npm run dev
```

Set the backend URL via `.env` (optional):
```
VITE_API_URL=http://localhost:8000
```

The chat UI calls `GET /query?q=` on the FastAPI backend.

